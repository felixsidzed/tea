import __auto func len(string str) -> int;
import __auto func eq(string s1, string s2) -> bool;

import __auto func itoa(int num, string buffer) -> string;
import __auto func cat(int nargs, ...) -> string; // result is on heap

import __auto func sub(string str, int i, int j) -> string; // result is on heap
