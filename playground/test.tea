import __cdecl func puts(string s) -> int;

public func main() -> int
	var x: int = -1;
	if (x > 0) do
		puts("positive\n");
	elseif (x < 0) do
		puts("negative\n");
	else
		puts("zero\n");
	end
	return 0;
end
