import __cdecl func puts(string s) -> int;

@inline
private func isNegative(int a) -> bool
	return a < 0;
end

public func main() -> int
	if (isNegative(-1)) do
		puts("Negative\n");
	else
		puts("Positive\n");
	end
	return 0;
end
