using "io";
using "sys";

public func main() -> int
	var start = sys::time();
	io::printf("time before: %llu\n", start);
	sys::sleep(500);
	var end_ = sys::time();
	io::printf("time after: %llu\n", end_);
	io::printf("slept for: %llu ms\n", end_ - start);

	sys::exit(67);
end
